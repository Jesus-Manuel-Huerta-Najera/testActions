print("hola")

def ordenar():
    lista = []
    while (count === "no" or count === "NO"):
        numero = input('agrega un numero entero')
        lista.append(numero)
        count = input('¿quieres continuar agregando?  si/no')
    return lista.sort()